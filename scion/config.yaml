# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

harness: cogo
image: scion-cogo:latest
user: scion

provisioner:
  type: container-script
  interface_version: 1
  command: ["python3", "/home/scion/.scion/harness/provision.py"]
  timeout: 30s
  lifecycle_events:
    - pre-start
  required_image_tools:
    - python3

config_dir: .agents
skills_dir: .agents/skills
interrupt_key: C-c
instructions_file: AGENTS.md
system_prompt_file: AGENTS.md
system_prompt_mode: prepend_to_instructions

command:
  base: ["cogo", "-yolo"]
  task_flag: "-p"
  task_position: after_base_args

env_template:
  SCION_AGENT_NAME: "{{ .AgentName }}"

mcp:
  global_config_file: .agents/mcp.json
  global_config_path: servers
  transport_field: transport
  transport_map:
    stdio: stdio
    sse: http
    streamable-http: http

capabilities:
  limits:
    max_turns: { support: "no", reason: "Cogo has no hook dialect for turn events" }
    max_model_calls: { support: "no", reason: "Cogo has no hook dialect for model events" }
    max_duration: { support: "yes" }
  telemetry:
    enabled: { support: "no", reason: "Cogo has no native OTEL integration yet" }
    native_emitter: { support: "no" }
  prompts:
    system_prompt: { support: "partial", reason: "System prompt is downgraded to AGENTS.md preamble" }
    agent_instructions: { support: "yes" }
  auth:
    api_key: { support: "yes" }
    auth_file: { support: "no" }
    oauth_token: { support: "no" }
    vertex_ai: { support: "yes", reason: "Vertex AI mode using Application Default Credentials" }
  mcp:
    stdio: { support: "yes" }
    sse: { support: "yes" }
    streamable_http: { support: "yes" }
    project_scope: { support: "no", reason: "Cogo demotes project-scoped MCP to global" }

no_auth:
  behavior: drop-to-shell
  message: |
    This agent started without credentials.
    Set GOOGLE_API_KEY for the Gemini API, or configure Vertex AI.

auth:
  default_type: api-key
  types:
    api-key:
      required_env:
        - any_of: ["GOOGLE_API_KEY"]
    vertex-ai:
      required_env:
        - any_of: ["GOOGLE_CLOUD_PROJECT"]
        - any_of: ["GOOGLE_CLOUD_LOCATION"]
  autodetect:
    env:
      GOOGLE_API_KEY: api-key
      GOOGLE_CLOUD_PROJECT: vertex-ai
